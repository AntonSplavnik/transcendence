services:
  transcendence:
    build: .
    ports:
      - "8080:8080"
      - "8443:8443"
      - "8443:8443/udp"
    volumes:
      - db-data:/app/data
    env_file:
      - .env
volumes:
  db-data:
