Q: does every module need to be in node.js fastify if we choose that module?
A: not necessary anymore

Q: can we write modules in any language that works, unless specified in the module?
A: yes, now we can

Q: multiplayer on one mashine (one keyboard) with guest user nickname?
A: yes. Remote module for multiplayer on separate PCs

Q: what are microservices in web development?
A: they are an Achitecture where every part that s topically or structurally it's own part, is deployed in it's own docker env.
